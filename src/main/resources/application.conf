akka {
  loglevel = INFO
}

spray.can.server {
  request-timeout = 1s
}

myPlay {
  profile = "slick.jdbc.MySQLProfile"
   db {
      dataSourceClassName = "slick.jdbc.DatabaseUrlDataSource"
      properties = {
        driver = "com.mysql.cj.jdbc.Driver"
        url = "jdbc:mysql://localhost:3306/myplay"
        user = "root"
        password = "sasa"
      }
    }
}
